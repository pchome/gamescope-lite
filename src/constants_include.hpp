#pragma once

#include <cstdint>

namespace defaults {

constexpr uint16_t nestedW = 1280;
constexpr uint16_t nestedH = 720;
constexpr uint16_t refreshHz = 60;
// constexpr uint16_t refreshmHz = 60000;

constexpr int minFSRSharpness = 0;
constexpr int maxFSRSharpness = 20;
constexpr int minNISSharpness = 0;
constexpr int maxNISSharpness = 20;

} // namespace defaults
